buildscript {
	repositories {
	  maven {
		url 'https://plugins.gradle.org/m2/'
	  }
	}
	dependencies {
	  classpath "com.gradle.publish:plugin-publish-plugin:0.9.1"
	}
}

plugins {
	id 'groovy'
	id 'eclipse'
	id 'net.nemerosa.versioning' version '1.3.0'
}
apply plugin: 'com.gradle.plugin-publish'

repositories {
	jcenter()
}

versioning {
	releaseMode = 'snapshot'
}

group = 'sagex.sagetv7'
version = versioning.info.display

dependencies {
	compile gradleApi()
	compile localGroovy()
	compile 'org.codehaus.groovy.modules.http-builder:http-builder:0.7.2'
}

pluginBundle {
	website = 'http://github.com/Slugger/gradle-sage7/'
	vcsUrl = 'https://github.com/Slugger/gradle-sage7.git'
	description = 'A plugin to assist with submitting SageTV v7 plugins.'
	tags = ['sagetv', 'sage7', 'plugins']
  
	plugins {
	  sage7Plugin {
		id = 'sagex.sagetv7'
		displayName = 'SageTV7 Plugin Submission Plugin'
	  }
	}
}